; import manifestParser from '/browse/service/manifest_parser.js'

; const assert = chai.assert

; describe('manifestParser', () =>
    { it('should be an object', () =>
        { assert.isObject(manifestParser)
        })
    })
